// Chatbot system prompt — updated with full website knowledge.

const SYSTEM_PROMPT = `তুমি মাহবুব সরদার সবুজের অফিসিয়াল ওয়েবসাইটের AI সহকারী। তোমার নাম "সহকারী"। তুমি বাংলায় কথা বলো এবং পাঠকদের সাথে আন্তরিক ও উষ্ণ ব্যবহার করো।

## লেখক পরিচয়
মাহবুব সরদার সবুজ একজন বাংলা ভাষার লেখক ও কবি। তিনি ভালোবাসা, বিচ্ছেদ, জীবনসংগ্রাম, স্মৃতি এবং মানবিক অনুভূতিকে সহজ অথচ আবেগঘন ভাষায় প্রকাশ করেন। তিনি কুমিল্লা জেলার বরুড়া উপজেলার খোশবাস ইউনিয়নের আরিফপুর গ্রামের সরদার বাড়িতে জন্মগ্রহণ করেন। কর্মসূত্রে বর্তমানে সৌদি আরবে অবস্থান করছেন। ২০১৫ সাল থেকে সামাজিক মাধ্যমে লেখালেখি শুরু করেন। তাঁর ফেসবুক পেজে ১ লক্ষ ১০ হাজারেরও বেশি ফলোয়ার রয়েছে।

## প্রকাশিত বই ও ই-বুক সমূহ

### ১. আমি বিচ্ছেদকে বলি দুঃখবিলাস (ফিজিক্যাল বই)
- ধরন: ফিজিক্যাল বই (প্রথম মুদ্রিত বই)
- বিষয়: আবেগী সাহিত্য
- প্রকাশ: ২০২৬
- পৃষ্ঠা: ১৫০+
- বিবরণ: বিচ্ছেদের ব্যথা, হারানোর কষ্ট আর জীবনের গভীর অনুভূতিগুলো এই বইয়ে অনন্যভাবে তুলে ধরা হয়েছে। প্রতিটি পাতায় লুকিয়ে আছে এক অন্যরকম ভালোবাসার গল্প — যে ভালোবাসায় মিশে আছে বিচ্ছেদের নীল বেদনা, অপেক্ষার দীর্ঘ রাত আর না-পাওয়ার হাহাকার।
- কোথায় পাবেন: রকমারি থেকে অর্ডার করুন — https://rkmri.co/TTMEoA3l3pM0/
- পড়ার লিংক: [BUTTON:/ebooks/read/dukkhovilash]
- বই পেজ: [BUTTON:/ebooks]

### ২. স্মৃতির বসন্তে তুমি (ই-বুক)
- ধরন: ই-বুক (বিনামূল্যে পড়া যায়)
- বিষয়: কবিতা ও গদ্য
- প্রকাশ: ২০২৪
- পৃষ্ঠা: ৮০+
- বিবরণ: স্মৃতির গভীরে হারিয়ে যাওয়া প্রিয় মুহূর্তগুলো নিয়ে লেখা এই বইটি। চাঁদের আলোয় ভেজা রাত, পুকুরের জলে পদ্মপাতা, শৈশবের সেই মেঠোপথ — সব মিলিয়ে এক অসাধারণ কাব্যিক যাত্রা।
- পড়ার লিংক: [BUTTON:/ebooks/read/smritir-boshonte]

### ৩. চাঁদফুল (ই-বুক)
- ধরন: ই-বুক (বিনামূল্যে পড়া যায়)
- বিষয়: কবিতা
- প্রকাশ: ২০২৩
- পৃষ্ঠা: ৬০+
- বিবরণ: প্রকৃতির অপরূপ সৌন্দর্য আর মানবমনের কোমল অনুভূতির মেলবন্ধন। চাঁদের আলো আর ফুলের সুবাস মিলিয়ে তৈরি এক অনন্য সাহিত্যকর্ম।
- পড়ার লিংক: [BUTTON:/ebooks/read/chand-phool]

### ৪. সময়ের গহ্বরে (ই-বুক)
- ধরন: ই-বুক (বিনামূল্যে পড়া যায়)
- বিষয়: গদ্য ও কবিতা
- প্রকাশ: ২০২৩
- বিবরণ: সময়ের স্রোতে হারিয়ে যাওয়া শহর, মানুষ আর স্মৃতির কথা। পুরনো শহরের গলিপথ, রেলস্টেশনের ঘড়ি, বাজারের কোলাহল, নদীর ধারের সন্ধ্যা — এক অসাধারণ নস্টালজিক সাহিত্যযাত্রা।
- পড়ার লিংক: [BUTTON:/ebooks/read/shomoyer-gohvore]

### ৫. মাহবুব সরদার সবুজের অনবদ্য লেখা (ই-বুক)
- ধরন: ই-বুক (বিনামূল্যে পড়া যায়)
- বিষয়: মিশ্র সাহিত্য
- বিবরণ: ১০০টি জীবনমুখী ও অনুপ্রেরণামূলক লেখার সংকলন। ভালোবাসা, বিচ্ছেদ, জীবনদর্শন ও মানবিক অনুভূতির মিশ্রণে রচিত।
- পড়ার লিংক: [BUTTON:/ebooks/read/onoboddo-lekha]

## ওয়েবসাইটের পেজসমূহ

### হোম পেজ
ওয়েবসাইটের মূল পেজ। লিংক: [BUTTON:/]

### পরিচিতি পেজ
লেখকের বিস্তারিত পরিচয়, জীবনের টাইমলাইন ও দর্শন। লিংক: [BUTTON:/about]

### লেখালেখি ও বই পেজ
সকল বই ও ই-বুকের সংগ্রহ, ক্যাটাগরি অনুযায়ী লেখা (ছোট লেখা, কবিতা, ভালোবাসা, জীবনদর্শন, বিচ্ছেদ)। লিংক: [BUTTON:/writings]

### ই-বুক পেজ
সকল বই ও ই-বুকের তালিকা। লিংক: [BUTTON:/ebooks]

### আবৃত্তি পেজ
মাহবুব সরদার সবুজের ৯টি নির্বাচিত আবৃত্তির সংকলন — Facebook-এ দেখা যায়। আবৃত্তিগুলো হলো: জানেন বাবা, আমি কাঁদলে মা আর কাঁদে না, তবুও তাকে ভালো, আমি জানি সব ঠিক হয়ে যাওয়ার একটা নিয়ম আছে, মাঝে মাঝে ইচ্ছে হয় তোমাকে ডেকে বলি, নারীকে ভালোবাসার আগে, মানুষটা তোমার প্রতি অন্ধ, এমনভাবে সরে যাবো একদিন, বিবেকের আদালত। লিংক: [BUTTON:/facebook-recitations]

### আমিও লিখবো বাস্তবতা পেজ
একটি সোশ্যাল ফিড প্ল্যাটফর্ম যেখানে যে কেউ নিজের বাস্তব গল্প, অভিজ্ঞতা, ভাবনা, কবিতা, ছবি ও ভিডিও শেয়ার করতে পারেন। লগইন করে পোস্ট করা যায়। লিংক: [BUTTON:/amio-likhbo-bastobota]

### ডিজাইন ফরম্যাট (সরদার ডিজাইন স্টুডিও)
একটি অনলাইন ডিজাইন এডিটর যেখানে কবিতার কার্ড, উদ্ধৃতি কার্ড তৈরি করা যায়। টুলস: ক্যানভাস, সরান, ক্রপ, PIP, ব্যাকগ্রাউন্ড, থিম, ফিল্টার, অ্যাডজাস্ট, লেখা, টেক্সট, স্টিকার, ড্রয়িং, আপস্কেল। লিংক: [BUTTON:/editor]

### গ্যালারি পেজ
লেখকের জীবনের বিশেষ মুহূর্ত, শিল্পকর্ম ও স্মৃতির ৪১টি ছবির সংগ্রহ। লিংক: [BUTTON:/gallery]

### সরদার সংবাদ পেজ
বাংলা সাহিত্য, বিনোদন, সমাজ, ভ্রমণ, ব্যক্তিত্ব, প্রযুক্তি, অর্থনীতি, আন্তর্জাতিক, রাজনীতি, জাতীয় ও সাফল্য বিষয়ক সংবাদ। লিংক: [BUTTON:/news]

### যোগাযোগ পেজ
লেখকের সাথে যোগাযোগের সকল তথ্য। লিংক: [BUTTON:/contact]

## যোগাযোগ তথ্য
- ইমেইল: lekhokmahbubsardarsabuj@gmail.com
- Facebook: https://facebook.com/Lekhok.MahbubSardarSabuj
- Instagram: https://instagram.com/mahbub_sardar_sabuj
- YouTube: https://youtube.com/@MahbubSardarSabuj
- অবস্থান: সৌদি আরব

## লেখকের দর্শন ও উক্তি
- "কলমের স্পর্শে আমি বিদ্রোহী, ন্যায়ের পক্ষে সদা প্রফুল্লচিত্তে ছুটি।"
- "মানুষের আচরণই আসল পরিচয় — কে কীভাবে কথা বলে, কেমন ব্যবহারে আগলে রাখে।"
- "পাঠকের ভালোবাসাই আমার লেখার শক্তি।"
- "লেখালেখি আমার কাছে শুধু শখ নয়; এটি আত্মপ্রকাশ ও পাঠকের সঙ্গে মানসিক সংযোগ।"

## গুরুত্বপূর্ণ নির্দেশনা
- সবসময় বাংলায় উত্তর দাও, যদি না ব্যবহারকারী ইংরেজিতে প্রশ্ন করেন।
- পাঠকদের সাথে আন্তরিক ও উষ্ণ ব্যবহার করো।
- বই বা ই-বুকের কথা উল্লেখ করলে সংশ্লিষ্ট [BUTTON:...] লিংক দাও।
- ওয়েবসাইটের বাইরের বিষয়ে প্রশ্ন করলে বিনয়ের সাথে জানাও যে তুমি শুধু এই ওয়েবসাইট সম্পর্কিত তথ্য দিতে পারো।
- রকমারি থেকে "দুঃখবিলাস" বইটি কেনার লিংক: https://rkmri.co/TTMEoA3l3pM0/
- ই-বুকগুলো সম্পূর্ণ বিনামূল্যে পড়া যায়।`;

function extractUserText(messages = []) {
  const lastUserMsg = [...messages].reverse().find((message) => message?.role === "user");
  if (!lastUserMsg) return "";
  if (Array.isArray(lastUserMsg.content)) {
    return lastUserMsg.content
      .map((part) => part?.type === "text" ? part.text : "")
      .filter(Boolean)
      .join(" ")
      .trim();
  }
  return String(lastUserMsg.content || "").trim();
}

function isEnglishQuery(text) {
  const letters = text.replace(/[^a-zA-Z\u0980-\u09FF]/g, "");
  const englishLetters = text.replace(/[^a-zA-Z]/g, "");
  if (letters.length === 0) return false;
  return (englishLetters.length / letters.length) > 0.6;
}

function buildFallbackReply(messages = [], aiError = null) {
  const userText = extractUserText(messages).toLowerCase();

  // বই সম্পর্কিত প্রশ্ন
  if (/বই|ebook|ই-বুক|দুঃখবিলাস|চাঁদফুল|স্মৃতির|সময়ের|অনবদ্য|কিনব|পড়ব|পড়তে/.test(userText)) {
    return `মাহবুব সরদার সবুজের বই সংগ্রহ দেখতে এখানে যান: [BUTTON:/ebooks]\n\nফিজিক্যাল বই "আমি বিচ্ছেদকে বলি দুঃখবিলাস" রকমারি থেকে অর্ডার করুন: https://rkmri.co/TTMEoA3l3pM0/\n\nই-বুকগুলো বিনামূল্যে পড়া যায়।`;
  }

  // যোগাযোগ সম্পর্কিত প্রশ্ন
  if (/যোগাযোগ|contact|ইমেইল|email|ফোন|phone|facebook|ফেসবুক/.test(userText)) {
    return `লেখকের সাথে যোগাযোগ করুন:\n📧 ইমেইল: lekhokmahbubsardarsabuj@gmail.com\n📘 Facebook: Lekhok.MahbubSardarSabuj\n📸 Instagram: mahbub_sardar_sabuj\n\nযোগাযোগ পেজ: [BUTTON:/contact]`;
  }

  // পরিচিতি সম্পর্কিত প্রশ্ন
  if (/কে|পরিচয়|about|লেখক|কবি|জন্ম|কুমিল্লা|সৌদি/.test(userText)) {
    return `মাহবুব সরদার সবুজ একজন বাংলা ভাষার লেখক ও কবি। কুমিল্লার বরুড়া উপজেলার আরিফপুর গ্রামে জন্মগ্রহণ করেন। বর্তমানে সৌদি আরবে কর্মরত। বিস্তারিত জানতে: [BUTTON:/about]`;
  }

  // আবৃত্তি সম্পর্কিত প্রশ্ন
  if (/আবৃত্তি|recitation|কণ্ঠ|শুনতে/.test(userText)) {
    return `মাহবুব সরদার সবুজের ৯টি আবৃত্তি শুনতে পারবেন এখানে: [BUTTON:/facebook-recitations]`;
  }

  // সংবাদ সম্পর্কিত প্রশ্ন
  if (/সংবাদ|news|খবর/.test(userText)) {
    return `সর্বশেষ সংবাদ পড়তে সরদার সংবাদ পেজে যান: [BUTTON:/news]`;
  }

  // ডিজাইন সম্পর্কিত প্রশ্ন
  if (/ডিজাইন|design|কার্ড|editor|এডিটর/.test(userText)) {
    return `সরদার ডিজাইন স্টুডিওতে কবিতার কার্ড তৈরি করুন: [BUTTON:/editor]`;
  }

  return "দুঃখিত, এই মুহূর্তে AI সেবা অনুপলব্ধ। অনুগ্রহ করে কিছুক্ষণ পর আবার চেষ্টা করুন অথবা সরাসরি যোগাযোগ করুন: [BUTTON:/contact]";
}



// Uses OPENAI_API_KEY and optional OPENAI_BASE_URL / OPENAI_MODEL environment variables
import {
  checkRateLimit,
  isProbablySpamText,
  limitJsonBodySize,
  normalizeText,
} from "./_utils/security.js";

// AI response থেকে raw URL গুলো [BUTTON] ট্যাগে রূপান্তর করা
function sanitizeReply(reply) {
  if (!reply || typeof reply !== "string") return reply;
  // Markdown লিংক [text](https://mahbubsardarsabuj.com/path) → [BUTTON:/path]
  reply = reply.replace(/\[([^\]]+)\]\(https?:\/\/(?:www\.)?mahbubsardarsabuj\.com(\/[^\)]*)?\)/g, (_, text, path) => {
    return path ? `[BUTTON:${path}]` : `[BUTTON:/]`;
  });
  // Raw URL https://mahbubsardarsabuj.com/path → [BUTTON:/path]
  reply = reply.replace(/https?:\/\/(?:www\.)?mahbubsardarsabuj\.com(\/[^\s\)\"\']+)?/g, (_, path) => {
    return path ? `[BUTTON:${path}]` : `[BUTTON:/]`;
  });
  // Typo URL https://mahmubsardarsabuj.com/path → [BUTTON:/path]
  reply = reply.replace(/https?:\/\/(?:www\.)?mahmubsardarsabuj\.com(\/[^\s\)\"\']+)?/g, (_, path) => {
    return path ? `[BUTTON:${path}]` : `[BUTTON:/]`;
  });
  return reply;
}


// Call AI API. It tries every configured provider before using the built-in natural fallback.
async function callAI(messages) {
  const configs = resolveAiConfigs();
  if (configs.length === 0) {
    throw new Error("No AI API key configured. Set GEMINI_API_KEY for the simplest production setup.");
  }
  let lastError;
  for (const config of configs) {
    try {
      return await callAIWithConfig(messages, config);
    } catch (err) {
      lastError = err;
      console.error(`AI provider ${config.source} failed; trying next provider if available:`, err.message);
    }
  }
  throw lastError || new Error("All AI providers failed");
}

function escapeTelegramHtml(value = "") {
  return String(value)
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;");
}

function truncateTelegramText(value = "", maxLength = 3500) {
  const text = String(value);
  if (text.length <= maxLength) return text;
  return text.slice(0, maxLength - 20) + "\n…[truncated]";
}


// Send photo to Telegram (base64 data URL)
async function sendPhotoToTelegram(botToken, adminChatId, base64Data, caption) {
  const matches = base64Data.match(/^data:(.+);base64,(.+)$/s);
  if (!matches) return { ok: false, error: "Invalid image data" };
  const mimeType = matches[1];
  const buffer = Buffer.from(matches[2], "base64");
  const ext = mimeType.split("/")[1] || "jpg";
  const boundary = "----FormBoundary" + Math.random().toString(36).slice(2);
  const CRLF = "\r\n";
  const parts = [];
  parts.push(`--${boundary}${CRLF}Content-Disposition: form-data; name="chat_id"${CRLF}${CRLF}${adminChatId}`);
  if (caption) {
    parts.push(`--${boundary}${CRLF}Content-Disposition: form-data; name="caption"${CRLF}Content-Type: text/plain; charset=utf-8${CRLF}${CRLF}${caption}`);
    parts.push(`--${boundary}${CRLF}Content-Disposition: form-data; name="parse_mode"${CRLF}${CRLF}HTML`);
  }
  const headerStr = parts.join(CRLF) + CRLF;
  const fileHeader = `--${boundary}${CRLF}Content-Disposition: form-data; name="photo"; filename="photo.${ext}"${CRLF}Content-Type: ${mimeType}${CRLF}${CRLF}`;
  const footer = `${CRLF}--${boundary}--`;
  const headerBuf = Buffer.from(headerStr, "utf-8");
  const fileHeaderBuf = Buffer.from(fileHeader, "utf-8");
  const footerBuf = Buffer.from(footer, "utf-8");
  const body = Buffer.concat([headerBuf, fileHeaderBuf, buffer, footerBuf]);
  const res = await fetch(`https://api.telegram.org/bot${botToken}/sendPhoto`, {
    method: "POST",
    headers: { "Content-Type": `multipart/form-data; boundary=${boundary}` },
    body,
  });
  return res.json().catch(() => ({}));
}

async function notifyTelegram({ userMessage, aiResponse, clientIp, userAgent, imageData }) {
  const botToken = process.env.TELEGRAM_BOT_TOKEN?.trim();
  const adminChatId = process.env.TELEGRAM_ADMIN_CHAT_ID?.trim();

  if (!botToken || !adminChatId) {
    console.warn("Telegram notification skipped: TELEGRAM_BOT_TOKEN or TELEGRAM_ADMIN_CHAT_ID is not configured.");
    return { ok: false, skipped: true, reason: "missing_env" };
  }

  const text = [
    "🤖 <b>AI Chatbot Conversation</b>",
    "",
    "<b>Visitor:</b> " + escapeTelegramHtml(truncateTelegramText(userMessage, 1200)),
    "",
    "<b>AI Reply:</b> " + escapeTelegramHtml(truncateTelegramText(aiResponse, 1800)),
    "",
    "<b>IP:</b> " + escapeTelegramHtml(clientIp || "unknown"),
    "<b>User Agent:</b> " + escapeTelegramHtml(truncateTelegramText(userAgent || "unknown", 400)),
    "<b>Time:</b> " + escapeTelegramHtml(new Date().toLocaleString("bn-BD", { timeZone: "Asia/Dhaka" })),
  ].join("\n");

  try {
    if (imageData && imageData.startsWith("data:")) {
      await sendPhotoToTelegram(botToken, adminChatId, imageData, text);
    } else {
      const response = await fetch("https://api.telegram.org/bot" + botToken + "/sendMessage", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          chat_id: adminChatId,
          text,
          parse_mode: "HTML",
          disable_web_page_preview: true,
        }),
      });
      const result = await response.json().catch(() => ({}));
      if (!response.ok || result.ok === false) {
        console.error("Telegram notification failed:", {
          status: response.status,
          description: result.description || "Unknown Telegram API error",
        });
        return { ok: false, status: response.status, description: result.description };
      }
    }
    return { ok: true };
  } catch (error) {
    console.error("Telegram notification failed:", error);
    return { ok: false, error: error.message };
  }
}

export default async function handler(req, res) {
  res.setHeader("Access-Control-Allow-Origin", "*");
  res.setHeader("Access-Control-Allow-Methods", "POST, OPTIONS");
  res.setHeader("Access-Control-Allow-Headers", "Content-Type");
  res.setHeader("Cache-Control", "no-store");

  if (req.method === "OPTIONS") return res.status(200).end();
  if (req.method !== "POST") return res.status(405).json({ error: "Method not allowed" });

  if (limitJsonBodySize(req, res, 2 * 1024 * 1024)) return;

  const rate = checkRateLimit(req, res, {
    keyPrefix: "chat",
    windowMs: 60 * 1000,
    max: 20,
  });
  if (rate.limited) return;

  try {
    const { messages } = req.body || {};

    if (!messages || !Array.isArray(messages) || messages.length === 0) {
      return res.status(400).json({ error: "Invalid messages" });
    }

    const lastUserContent = messages
      .filter((message) => message?.role === "user")
      .slice(-1)[0]?.content;
    const lastUserText = Array.isArray(lastUserContent)
      ? lastUserContent.find((part) => part?.type === "text")?.text || ""
      : lastUserContent || "";

    if (normalizeText(lastUserText, 5000).length > 4000 || isProbablySpamText(lastUserText)) {
      return res.status(400).json({ error: "আপনার বার্তাটি খুব বড় বা সন্দেহজনক। অনুগ্রহ করে সংক্ষিপ্ত ও স্বাভাবিক বার্তা পাঠান।" });
    }

    // Filter out any system messages sent from the frontend (to avoid duplication)
    // and keep only the last 12 user/assistant messages for context
    const filteredMessages = messages
      .filter((m) => m.role !== "system" && ["user", "assistant"].includes(m.role))
      .slice(-12);
    const allMessages = [
      { role: "system", content: SYSTEM_PROMPT },
      ...filteredMessages,
    ];

    try {
      const reply = await callAI(allMessages);

      const lastUserMsg = messages.filter((message) => message.role === "user").slice(-1)[0];
      const lastUserImgPart = Array.isArray(lastUserMsg?.content)
        ? lastUserMsg.content.find(p => p.type === "image_url")?.image_url?.url
        : null;
      await notifyTelegram({
        userMessage: lastUserMsg ? (Array.isArray(lastUserMsg.content) ? lastUserMsg.content.find(p => p.type === 'text')?.text || '[ছবি পাঠানো হয়েছে]' : lastUserMsg.content) : "(অজানা)",
        aiResponse: reply,
        clientIp: rate.clientIp,
        userAgent: req.headers["user-agent"],
        imageData: lastUserImgPart || null,
      });

      return res.status(200).json({ reply: sanitizeReply(reply) });
    } catch (err) {
      console.error("AI API failed; returning built-in fallback reply:", err.message);
      const fallbackReply = buildFallbackReply(messages, err);
      const lastUserMsg = messages.filter((message) => message.role === "user").slice(-1)[0];
      const lastUserImgPart = Array.isArray(lastUserMsg?.content)
        ? lastUserMsg.content.find(p => p.type === "image_url")?.image_url?.url
        : null;
      await notifyTelegram({
        userMessage: lastUserMsg ? (Array.isArray(lastUserMsg.content) ? lastUserMsg.content.find(p => p.type === 'text')?.text || '[ছবি পাঠানো হয়েছে]' : lastUserMsg.content) : "(অজানা)",
        aiResponse: `${fallbackReply}\n\n[Fallback used because AI provider failed: ${err.message}]`,
        clientIp: rate.clientIp,
        userAgent: req.headers["user-agent"],
        imageData: lastUserImgPart || null,
      }).catch((notifyError) => console.error("Telegram fallback notification failed:", notifyError.message));
      return res.status(200).json({ reply: fallbackReply, fallback: true });
    }
  } catch (err) {
    console.error("Chat handler error:", err);
    return res.status(500).json({ error: "সার্ভারে সমস্যা হয়েছে। অনুগ্রহ করে আবার চেষ্টা করুন।" });
  }
}
